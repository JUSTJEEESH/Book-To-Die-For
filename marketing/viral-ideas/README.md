# Viral Ideas

The master list is `../CONTENT_DATABASE.md`. This folder holds raw idea dumps from the daily
workflow before they are cleaned into the database: one file per day, `YYYY-MM-DD.md`. Anything
that survives the quality gate gets an ID and moves to the database.
